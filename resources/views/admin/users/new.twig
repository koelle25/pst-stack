{% extends 'templates/admin-layout.twig' %}

{% block content %}
    <div id="page-wrapper">
        <div id="content-wrapper">
            {% include "templates/partials/flash.twig" %}

            <div class="login-panel panel panel-{{ errors ? 'danger' : 'default' }}">
                <div class="panel-heading">
                    <h3 class="panel-title">Create new user</h3>
                </div>
                <form action="{{ path_for('admin.users.new') }}" method="post">
                    <div class="panel-body">
                        <fieldset>
                            <div class="row">
                                <div class="form-group col-md-6{{ errors.firstName ? ' has-error' : '' }}">
                                    <label class="control-label" for="firstName">First Name</label>
                                    <input name="firstName" type="text" class="form-control" id="firstName"
                                           placeholder="First Name" value="{{ old.firstName }}" autofocus>
                                    {% if errors.firstName %}
                                        <span class="help-block">{{ errors.firstName | first }}</span>
                                    {% endif %}
                                </div>
                                <div class="form-group col-md-6{{ errors.lastName ? ' has-error' : '' }}">
                                    <label class="control-label" for="lastName">Last Name</label>
                                    <input name="lastName" type="text" class="form-control" id="lastName"
                                           placeholder="Last Name" value="{{ old.lastName }}">
                                    {% if errors.lastName %}
                                        <span class="help-block">{{ errors.lastName | first }}</span>
                                    {% endif %}
                                </div>
                            </div>
                            <div class="row">
                                <div class="form-group col-md-6{{ errors.email ? ' has-error' : '' }}">
                                    <label class="control-label" for="email">E-Mail-Address</label>
                                    <input name="email" type="text" class="form-control" id="email"
                                           placeholder="E-Mail-Address" value="{{ old.email }}">
                                    {% if errors.email %}
                                        <span class="help-block">{{ errors.email | first }}</span>
                                    {% endif %}
                                </div>
                            </div>
                            {{ csrf.field | raw }}
                        </fieldset>
                    </div>
                    <div class="panel-footer">
                        <button type="submit" name="commit" class="btn btn-success">Create</button>
                        <button type="submit" name="continue" class="btn btn-default">Create and continue</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    </div>
{% endblock %}